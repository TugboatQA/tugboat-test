services:
  go:
    image: tugboatqa/alpine
    aliases: test
    http: true
    https: false
    expose: 8080
    subpath: true
    commands:
      init:
        - apk update
        - apk add --no-cache --virtual .build-deps runit bash gcc musl-dev openssl go
        - mkdir -p /etc/service/server
        - cp "${TUGBOAT_ROOT}/.tugboat/runit/server/run" /etc/service/server/run
        - chmod +x /etc/service/server/run
      update:
        - echo "********** UPDATE **********"
      build:
        - echo "********** BUILD **********"
        - cd ${TUGBOAT_ROOT}/server && go build -o server .
        - echo "this value is an encrypted global environment variable:" ${SECRET_ENV_VAR}
        - echo "this value is an encrypted runtime environment variable (SHOULD NOT BE VISIBLE):" ${ENCRYPTED_RUNTIME}
        - cat .tugboat/tugboat.txt
      online:
        - echo "********** ONLINE **********"
        - echo "this value is an encrypted global environment variable:" ${SECRET_ENV_VAR}
        - echo "this value is an encrypted runtime environment variable (SHOULD BE VISIBLE):" ${ENCRYPTED_RUNTIME}
        - cat .tugboat/tugboat.txt
